# import curses
# from cryptography.fernet import Fernet
# key = Fernet.generate_key()
# cipher_suite = Fernet(key)
# cipher_text = cipher_suite.encrypt(b"A really secret message. Not for prying eyes.")
# plain_text = cipher_suite.decrypt(cipher_text)

# import iocore
# from curses import wrapper as wrapperx
# import os
#
# def mainIonor(stdscr):
#     curses.nocbreak()
#     curses.echo()
#     print("height : {} and width : {} ".format(curses.LINES + 1, curses.COLS + 1))
# wrapperx(mainIonor)
#
import iocore

obj = iocore.ionor()
# obj.addr(platform="facebook",login="chris", password="christian")
# obj.upDateDeepStore()
